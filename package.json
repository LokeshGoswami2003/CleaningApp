{
  "name": "cleaning-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "client-web"
  ],
  "scripts": {
    "docs:journey": "powershell -ExecutionPolicy Bypass -File ./scripts/new-journey-entry.ps1 -Title",
    "build": "npm run build --workspace server && npm run build --workspace client-web",
    "test": "npm run test --workspace server",
    "lint": "npm run lint --workspace server && npm run lint --workspace client-web",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace client-web"
  }
}

